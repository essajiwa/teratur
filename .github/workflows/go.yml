name: Go Build Test Workflow
on: [pull_request]
jobs:

  build:
    name: Static Check
    runs-on: ubuntu-latest
    steps:
    - name: Check out code into the Go module directory
      uses: actions/checkout@master

    - name: Run Static Check
      uses: ./.github/staticcheck/
